# Code

## `apple_volatility_submitted_path_redacted.R`

Preserves the retained homework code while removing the machine-specific personal file path.

## `apple_volatility_portable.R`

Changes the data import to:

`data/APPL.xlsx`

and removes `install.packages()` from the analysis script.

No model-selection logic has been silently corrected.

Before treating the project as fully reproducible, review `../docs/REPRODUCIBILITY_NOTES.md`.
